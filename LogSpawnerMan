from msvcrt import setmode
import pygame, sys
from GameObject import GameObject
from GameObjects.Log import Log
from GameObjects.Player import Player
import random
from GameWorld import GameWorld
from LogSpawnPoint import LogSpawnPoint

class LogSpawnerMan():

    def __init__(self):
        self.spawnpoints = [LogSpawnPoint]

    def spawnLog():
        if (GameWorld._gameobjects.count < 10):
            lane = random.randint(0, 5)


    